config.stopBubbling = true
# Adds @lombok.Generated to generated methods so JaCoCo excludes getters/setters/
# builders from coverage — only hand-written logic needs tests.
lombok.addLombokGeneratedAnnotation = true
